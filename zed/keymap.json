// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
      "cmd-g": ["task::Spawn", { "task_name": "start lazygit", "reveal_target": "center" }],
      "cmd-p": ["task::Spawn", { "task_name": "Television File Finder", "reveal_target": "center" }]
    }
  },
  {
    "context": "Editor && mode == full",
    "bindings": {
      "cmd-f": ["task::Spawn", { "task_name": "Television Text Finder", "reveal_target": "center" }]
    }
  },
  {
    "context": "Pane",
    "bindings": {
      // Override existing keybinding for this context
      "cmd-g": ["task::Spawn", { "task_name": "start lazygit", "reveal_target": "center" }],
    }
  },
  {
    "context": "ContextEditor > Editor",
    "bindings": {
      // Override existing keybinding for this context
      "cmd-g": ["task::Spawn", { "task_name": "start lazygit", "reveal_target": "center" }],
    }
  }
]
